sort1 uses: bubble sort

How do you know?: when sorting random numbers sort1 was slower than both sort2 and sort3 

sort2 uses: merge sort

How do you know?: sort2 got the best time sorting random numbers, much faster than sort1 and sort3

sort3 uses: selection sort

How do you know?: when sorting random numbers sort3 was faster than sort1, but much slower than sort2. 
Selection sort is faster than Bubble sort for sorting a big array of unsorted numbers, and Merge Sort is faster than both in this case.
